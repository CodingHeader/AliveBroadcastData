# AliveBroadcastData — 抖音直播数据分析系统

## 功能
- 自动采集抖音来客线索大屏数据 (油猴脚本)
- 数据看板 (KPI+趋势+场次+漏斗)
- AI分析报告 (自动+手动)
- 邮件推送 (HTML日报)
- 后台管理 (成单/主播/AI/邮箱/系统设置)

## 快速开始

### 1. 启动服务端
- Windows: 双击 `start.bat`
- Linux: `chmod +x start.sh && ./start.sh`

### 2. 安装油猴脚本
1. 安装 Tampermonkey 浏览器插件
2. 打开 `tampermonkey/alive-broadcast-sync.user.js`
3. 修改 `SERVER_URL` 为你的服务器地址
4. 安装脚本，保持浏览器打开 life.douyin.com

### 3. 后台配置
1. 访问 http://YOUR_IP:12306/admin/login
2. 默认账号: admin / admin123
3. 配置AI (API Key + 模型) 和邮箱 (SMTP + 收件人)

## 技术栈
Python 3.11+ | FastAPI | SQLite | Jinja2 | TailwindCSS | Alpine.js | ECharts

## 定时任务
| 任务 | 时间 | 说明 |
|------|------|------|
| AI分析 | 每小时:05 | 自动分析未分析场次 |
| 日报生成 | 每天01:20 | 聚合前日数据(AI分析后) |
| 邮件推送 | 每天01:30 | 发送日报邮件 |
| 周报 | 每周一02:00 | 聚合上周数据+AI分析 |
| 月报 | 每月1号03:00 | 聚合上月数据 |
| 数据库备份 | 每天04:00 | 保留7天 |

## 常见问题

**Q: 油猴脚本安装后不触发采集？**
A: 确认浏览器保持打开life.douyin.com页面，检查控制台是否有"[直播同步] 脚本已加载"日志。如果没有，检查@match规则是否匹配当前URL。

**Q: 邮件发送失败？**
A: 163邮箱需要开启SMTP服务并获取授权码（非登录密码）。在163邮箱设置→POP3/SMTP中开启，获取授权码填入后台配置。

**Q: AI分析返回空或报错？**
A: 检查后台AI配置的API Key是否有效、base_url是否正确。可点击"测试连接"验证。Github Models的base_url为 https://models.github.ai/inference。

**Q: 数据库报"database is locked"？**
A: 系统已配置WAL模式，正常情况不会出现。如仍出现，重启服务即可。避免同时运行多个服务实例。

**Q: 端口12306被占用？**
A: 修改server/config.py中的PORT为其他端口（如12307），同时更新油猴脚本中的SERVER_URL。

**Q: 修改了.env环境变量但配置没生效？**
A: 环境变量仅在首次初始化data.db时写入settings表。如果data.db已存在，需要：
- 方案1：在后台管理页面手动修改对应配置
- 方案2：删除`server/data.db`后重启服务（会丢失所有数据）
- 方案3：用SQLite工具直接修改settings表中对应key的value


-----

## 新增排班功能

### 作用
- ** 计算工作时间 **
- ** 分析数据，体现主播能力 **
- ** 一键排班减少工作量 **

### 描述
一场直播大概率会有多个主播"接力"的阶段，因为主播不能像5、6个小时长时间的说话。每个阶段，可能存在多个主播，可能是一个主播在播，可能是2个主播在"双打"。参考我们的排班表示例` .docs/直播/Set_排班表.md `

我希望将排班系统纳入系统，每场直播的每个阶段，必须关联到有哪些主播、以及主播的性别、风格。

后台可以对排班表进行配置，我倾向于(1)后台设置排班方案。(2)后台在日历上？对每一天的日期绑定排班方案。

后台管理员配置排班方案时可通过按钮新增维度/选择，表格动态更新

关于"阶段"的时间粒度——排班表里是按 1小时 为一个时段单位。可以提供0.5小时；1小时；2小时；3小时等时间粒度。可以设置。默认是1小时。

直播间数量。同时间粒度一样，直播间默认是2个，但可以增加下拉框，后台管理员动态设置。

性别 ：男/女
风格 ：B ：自由文本，管理员自己填写


"日历上绑定排班方案"。日历顶部有周一到周天的列名，更清晰。
每天一个格子，点击某天弹出下拉选择排班方案。支持批量选择多天绑定同一方案（如选中周一到周五统一绑"工作日5人场"）


关于排班方案中的"休息"和"空"标记。排班方案必须体现出开播时间和关播时间。排班方案中有3种状态：关播休息时段、开播无主播时段、开播有主播时段。默认是"开播有主播"
排班表.md文件中，"-"代表停播，"[-]"代表是兼职主播上场做替换。


在后台配置排班方案时，首先要建立方案大纲，如方案名、开始时间、结束时间、是否有效(默认有效)、备注等等；才能配置排班方案，如类型(1直播间、2直播间、3直播间)、主播人数(1人、2人、3人、4人、5人、6人等)等可以在建立详细方案时调整。


在后台配置实际排班表时，必须要先配置主播的姓名和信息，然后才能配置实际排班表，配置实际排班表可以筛选字段值快速绑定排班方案。然后将主播1、主播2、主播3、主播4、主播5等，与实际主播关联起来就行。


1. 必须支持跨天排班，方案中的"关播时间"是否支持选次日时间（如"次日01:00"）。
2. 必须场次与排班自动关联 ：油猴脚本采集的场次数据，是否需要根据场次时间自动匹配当日排班，将 SessionAnchor 记录自动生成。
3. 如果修改了排班方案，已绑定的历史日期的排班需要实时读取最新方案。

-----

## 投流规则

每个抖音直播间对应的不同账户，需要可以绑定直播账户名称、账户ID。油猴脚本中，也需要设置对应的账户ID。每个抖音直播间，同一时间内，只能绑定一个账户，后续可以更换，比如1、2直播间互换账号。


每个直播间对应账户，都会有多个不同的投流计划。投流计划中字段很多，核心的是单元出价。而什么时候使用哪个计划，每个账户/每场直播/每个投流计划 花费了多少，会影响我们下一场投流计划的设计。

-----

## 直播数据采集
抖音直播数据的获得分为两部分。
1. 抖音开放平台API获得的简略线索数据。
2. 油猴脚本获得的单个账户&&单个直播间&&单场直播的详细直播数据。

区别1：第一部分能几乎实时获得有价值的线索，可以加密也可以解密。第二部分只能获得被加密的线索，但能获得大量其他直播数据。

区别2：第一部分能返回投流计划的部分信息，而第二部分不会返回任何投流信息。

**核心**：抖音开放平台API解密的次数是有限制的。所以必须先判定存在该线索，不存在才解密，通过线索ID判断。你必须参考`.docs\直播\Get_线索API_抖音.py`，`.docs\直播\Get_线索API_字段列表.md`，`.docs\直播\explian.md`。

要求：必须要分清，是哪个账户来的线索，根据线索创建时间和排班表判断来自哪个主播。
一般情况下，`.docs\直播\Get_线索API_抖音.py`保持在后台运行，更新时间早于油猴脚本。线索或其他信息的字段，需要能和API线索对应上。

我目前有3个抖音来客账户，2个直播间，相当于有一个账号是空置的。我是多账号矩阵号，每个账号多场次开播，也可能全天只播一场(长时间拉满)。后续会增加账户。

-----

## 数据图表看板
首页数据看板，总筛选栏现在有"日"、"周"、"月"。现在需要在日期选择器的左侧，新增一个"场"的选择器，默认是全部。每场的命名规则是"{开始时间} - {结束时间}"，时间是"HH:mm"格式。可以点击上一场或下一场，但没有上一场时，则点不动。

**核心区别** 点击中间的名称时，不能编辑，只有当日的场次下拉框可以选择。

** 特别注意：只有选择"日"时，才能呈现"场"选择器，否则隐藏。选了场之后，总场次为1，总线索、总消耗、线索平均成本等以该场的数据计算，包括历史日均线索、历史日均消耗改为历史场均线索、历史日均消耗。

我希望后台管理员能自行建立可视化报表，自由创建X轴和Y轴的指标和计算方式。

多报表呈现：将首页的<主播表现 Top3>和<线索趋势>修改成多报表呈现板块。

<主播表现 Top3><线索趋势>两个div板块视为一个整体。在它们上方有多个tab，每个tab都是一个可视化报表。<主播表现 Top3>更改为数据分析。<线索趋势>更改为"tab可视化图表"。<主播表现 Top3><线索趋势>上方新增tab行，点击tab切换数据。

** 注意 ** 该tab功能仍受场、日、周、月的时间粒度影响。

<主播表现 Top3><线索趋势>上方新增tab行。默认呈现第一个报表，第一个报表是总览，整体呈现：曝光人数和进入房间的人数的几率(曝光进入率)、进入房间人数和线索个数(留资率)、人均观看时长。该表不能后台配置，默认优先级是0。点击其他tab切换线索。

可选的图表类型默认是：折线图。X轴是时间粒度，Y轴是指标。指标可以是任意字段，如曝光人数、进入房间人数、线索个数等。
上方的数据分析由AI实时分析，流式输出。

指标可以由多个字段计算得来。
每个tab都需要可以配置系统提示词和用户提示词，不配置则使用默认的提示词。默认的提示词也在后台配置。
-----

## 数据推送和分配
使用API实时采集线索数据，当线索未被系统收录时，更新表格列。表格列可以筛选时间，默认呈现今天。

表格列有：分配时间、主播(多个主播用逗号隔开)、电话、微信、招生老师、有效性、反馈、备注

后台设置招生团队的队列，每个员工依次循环推送线索。比如社招部+5个员工+依次分配

推送到钉钉群，@对应同事后，同事在首页领取和备注。要有分配时间和领取记录。默认是隐藏的，点击显示必须输入密码后才能看到。后台可配置，每个招生团队的密码都不一样，第一个团队的密码是"hd1994"

"微信"列——这是线索自带的微信字段，但属于抖音直播的离线数据，不支持API，需要由管理员手动补充。

表格列--"主播"指的是一个或多个主播。比如热门时间段，可能开双人场，也可能开单人场。注意要把留资时间和主播的时间段对应上。

"领取"——是招生老师主动点击"领取"按钮确认领取，由他领取并处理，查看就算已领取。未领取呈现未领取，领取了显示跟进中。

钉钉推送，`.docs\直播\Sent_数据API_钉钉.py`代码写好了，测试直接推送。注意需要在后台配置webhook_url和secret以及发送内容，最后自行关联要@的同事。
同理线索API也要配置ACCOUNT_ID、CLIENT_KEY、CLIENT_SECRET

密码保护，仅电话/微信等敏感字段隐藏。输入密码后查看本条线索。点哪条，输密码，看哪条。可以在后台配置，不用输密码，点击后直接查看。默认不用输密码，由管理员配置。

"反馈"和"备注"，是两个字段，没有预设项。

循环分配的粒度。按部门/团队分配，依次循环分配，比如给A分了一个，接下来给B分一个，再给A分一个，再B...

同一线索API和脚本都能采集，但脚本无法解密！看到的联系方式是加密的！

同一线索API和脚本都能采集，但脚本无法解密！看到的联系方式是加密的！。API先采集线索，在使用油猴脚本采集后，先对照映射，再存入其他数据。比如当前API返回查询并解密到线索"13990592943"，而油猴脚本采集到"*******2943"。则视为同一个线索。


-----

## 主播表现和工作时间统计

可以根据多个时间粒度查看，比如一个月直播了多少个小时，有多少线索，线索有效条数是多少。

1. 一场直播的线索来自API和油猴脚本无法采集的抖音私信，需要人员主动添加。
2. 在某场直播中，人气很高，临时增加直播时间。

-----



## 修正历史

1. 场次选择应该是完整的:开始时间到结束时间.找到所有相关部分,准备修改方案.
2. 主播配置点击新增无效.
3. 广告账户又是什么.
4. 点击投流计划--新增计划没有反应.
5. API线索账号配置,轮询时间应该是按秒计算.默认每30秒轮询一次.

本次任务禁止编辑和新建文件。依次分析下列问题，对每个问题找到对应的代码文件结构，思考每个问题的解决方案。



1. 新增主播时，必须点击新增按钮，才能弹出表单信息。并且还能删除和修改表单。
2. 招生团队处，新增员工，为什么没有员工姓名的输入框，第一列是什么，又小又不能输入。
3. 线索管理页面中("/admin/leads")，点击标记成单没有反应。标记成单后，必须可以取消标记。
4. 线索管理页面中("/admin/leads")，添加私信线索无效。
5. 线索管理页面中("/admin/leads")，筛选后没有反应.

- 招生人员编辑和修改线索有效性。
- 在某场直播中，人气很高，临时增加直播时间。
- 一场直播的线索来自API和油猴脚本无法采集的抖音私信，需要人员主动添加，极可能类型不是来自"抖音直播"，而是来自"自然流"。
​

新建的排班方案，全部默认开播有主播，方便管理员配置。

排班日志中，当前时间应该在最中间，向前/向后跳转时间应该在左右两边。

## 待解决
1. 排班表显示：开播有主播
2. 添加主播时，提示服务器内部错误。
3. 可以补充和修改过去时间(比如上周)的排班表和日历。对历史直播补充各主播的排班表。
4. 统计数据。油猴脚本及私信分析。
5. 重新采集数据，自然流数据手动采集。
6. 线索实时提醒。新留资线索发送钉钉。
7. 判断评论是否和咨询业务相关，获得评论留资率(评论留资数/评论咨询数)。
8. 增加图表维度。私信人数、私信留资数；
评论人数、评论留资数。如果没有相关数据，以"-"填充单元格。
9. 后台新建单独页面--主播月薪计算器。第一行为计算参数(含"计算"和"导出"按钮)，第二行为筛选栏，下方是表格。底薪？元/小时+线索？元/条+广告流线索？元/条+自然流线索？元/条。底薪和线索使用输入框动态配置，点击计算后以表格方式输出主播的月薪明细，表格以每天为一行，包括日期、直播时间(时/分)、直播时段(多个时段以"/"隔开，如08:00-10:00/12:00-14:00)、线索数，可以下拉切换主播，可以切换月份，也可以导出为excel。

当计算后，显示所有的主播月薪概览(直播时长、线索数、广告流线索数、自然流线索数)。点击某主播详情后，查看上述表格。

-----
如果我想统计关键数据，输出为表格，表格列名为[日期，直播场次，主播场次，关联主播，关联场次，主播场次，直播开始时间 - 结束时间，主播上播时间 - 下播时间，评论数，评论获客数，自然流留资数，广告流留资数，总留资数]。

我希望完成主播时薪(40元/小时)计算月收入，获客提成计算(仅计算广告流留资数)。是否抓住评论获客，获客率多少，及其他分析数据。

仔细思考：是否有缺失字段，是否完整，本系统功能是否能实现，如何实现。油猴脚本的私信分析是否采集。

"主播上播时间 - 下播时间" 、"主播场次"、"关联场次"与主播排班表对应。假如一天有3个主播，每个主播都有自己的上下播时间，主播场次指的是每天的第几场，关联场次为"上午场、下午场、晚间场"3选1，按上播时间决定。

关联场次按上播时间决定。
06点-12点为上午场；12点-18点为下午场；18点-06点为晚间场；

## 示例
今天直播了8小时。甲直播2小时，乙直播2小时，丙直播2小时，甲直播2小时。
那么，4行数据示例
"""
日期 | 主播上播时间 - 下播时间 | 主播场次 | 关联场次
6月9日 | 08:00-10:00 | 1 | 上午场 |
6月9日 | 10:00-12:00 | 2 | 上午场 |
6月9日 | 12:00-14:00 | 3 | 下午场 |
6月9日 | 14:00-16:00 | 4 | 下午场 |
"""

-----

添加线索的类型增加一个营销流，可选项营销流排第一，自然流排第二。
检查所有界面，筛选条件在同一行。筛选条件的各元素可以适当缩小。
检查所有列表/表格，列名必须呈一行。比如当前线索管理界面，分配员工列，"分配员"在一行，"工"在第二行，很不好看。
当有弹出框时，点击界面其他地方，可以取消弹出框展示。
